version: "3"
services:
  database:
    image: "postgres"
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=1234
      - POSTGRES_DB=workgrade
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/db.sql